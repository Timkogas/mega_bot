<!DOCTYPE html>
<html lang="en">

<head>
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.10.24/css/jquery.dataTables.css">
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/rowgroup/1.1.3/css/rowGroup.dataTables.min.css">
    <style>
        /* Ваш CSS остается неизменным */

        body {
            font-family: Arial, Helvetica, sans-serif;
            margin: 20px;
        }

        h1,
        h2 {
            color: #333;
        }

        div {
            margin-bottom: 20px;
        }

        table {
            border-collapse: collapse;
            width: 100%;
        }

        th,
        td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
        }

        tr:hover {
            background-color: #f5f5f5;
        }

        .pagination {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }

        .pagination a {
            color: black;
            padding: 8px 16px;
            text-decoration: none;
            transition: background-color .3s;
            margin: 0 4px;
        }

        .pagination a.active {
            background-color: #4CAF50;
            color: white;
        }

        .pagination a:hover:not(.active) {
            background-color: #ddd;
        }
    </style>
</head>

<body style="font-family: Arial, Helvetica, sans-serif;">

    <h1>Статистика</h1>

    <h2>Результаты сессий сканирования чеков</h2>

    <table id="statsTable" class="display">
        <thead>
            <tr>
                <th data-i18n="Platform">Платформа</th>
                <th data-i18n="Channel">Канал/Аккаунт</th>
                <th data-i18n="Creative">Креатив</th>
                <th data-i18n="Total Launches">Общее количество запусков чат-бота</th>
                <th data-i18n="Unique Users">Количество уникальных пользователей, запустивших чат-бот</th>
                <th data-i18n="Total Web App Launches">Общее количество запусков Web-App</th>
                <th data-i18n="Unique Web App Users">Количество уникальных пользователей, запустивших Web-App</th>
                <th data-i18n="Game Sessions">Количество пользователей, завершивших игровую сессию</th>
            </tr>
        </thead>
        <tbody>
            <% for (const item of userActivityStats) { %>
                <tr data-platform="<%= item.id || '' %>" class="platform">
                    <td><%= item.id || '' %></td>
                    <td></td>
                    <td></td>
                    <td><%= item.total_launches %></td>
                    <td><%= item.unique_users %></td>
                    <td><%= item.total_web_app_launches %></td>
                    <td><%= item.unique_web_app_users %></td>
                    <td><%= item.game_sessions %></td>
                </tr>
                <% for (const channel of item.channels) { %>
                    <tr data-platform="<%= item.id || '' %>" data-channel="<%= channel.id || '' %>" class="channel">
                        <td></td>
                        <td><%= channel.id || '' %></td>
                        <td></td>
                        <td><%= channel.total_launches %></td>
                        <td><%= channel.unique_users %></td>
                        <td><%= channel.total_web_app_launches %></td>
                        <td><%= channel.unique_web_app_users %></td>
                        <td><%= channel.game_sessions %></td>
                    </tr>
                    <% for (const creative of channel.creatives) { %>
                        <tr data-platform="<%= item.id || '' %>" data-channel="<%= channel.id || '' %>" data-creative="<%= creative.id || '' %>" class="creative">
                            <td></td>
                            <td></td>
                            <td><%= creative.id || '' %></td>
                            <td><%= creative.total_launches %></td>
                            <td><%= creative.unique_users %></td>
                            <td><%= creative.total_web_app_launches %></td>
                            <td><%= creative.unique_web_app_users %></td>
                            <td><%= creative.game_sessions %></td>
                        </tr>
                    <% } %>
                <% } %>
            <% } %>
        </tbody>
    </table>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.datatables.net/1.10.24/js/jquery.dataTables.min.js"></script>
    <script src="https://cdn.datatables.net/rowgroup/1.1.3/js/dataTables.rowGroup.min.js"></script>
    <script src="https://cdn.datatables.net/plug-ins/1.10.24/i18n/Russian.json"></script>
    <script>
        $(document).ready(function () {
            var table = $('#statsTable').DataTable({
                order: [
                    [0, 'asc'],
                    [1, 'asc'],
                    [2, 'asc']
                ],
                rowGroup: {
                    dataSrc: ['platform', 'channel', 'creative'],
                    startRender: function (rows, group) {
                        return $('<tr/>')
                            .append('<td colspan="8">' + group + ' (' + rows.count() + ' items)</td>');
                    }
                },
                language: {
                    url: 'https://cdn.datatables.net/plug-ins/1.10.24/i18n/Russian.json'
                }
            });
        });
    </script>

</body>

</html>